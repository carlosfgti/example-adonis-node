<nav>
    <ul class="pagination">
        @if(products.page != '1')
        <li class="page-item">
            <a class="page-link" href="?page={{ products.page - 1 }}">Previous</a>
        </li>
        @endif
    
        @if(products.page != products.lastPage)
        <li class="page-item">
            <a class="page-link" href="?page={{ products.page + 1 }}">Next</a>
        </li>
        @endif    
    </ul>
</nav>